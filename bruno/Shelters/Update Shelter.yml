info:
  name: Update Shelter
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/shelters/1"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "name": "Paws PH Updated",
        "location": "BGC, Taguig"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
