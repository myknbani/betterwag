info:
  name: Get All Shelters
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/shelters"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
