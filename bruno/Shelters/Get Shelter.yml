info:
  name: Get Shelter
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/shelters/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
