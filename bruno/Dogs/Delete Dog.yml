info:
  name: Delete Dog
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/dogs/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
