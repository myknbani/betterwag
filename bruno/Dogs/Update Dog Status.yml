info:
  name: Update Dog Status
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/dogs/1/status"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "adoption_status": "adopted"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
