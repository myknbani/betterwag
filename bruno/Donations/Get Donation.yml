info:
  name: Get Donation
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/donations/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
