info:
  name: Update Campaign
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/campaigns/1"
  body:
    type: json
    data: |-
      {
        "title": "Help us keep the lights on (updated)"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
