info:
  name: Get Shelter Campaigns
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/shelters/1/campaigns"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
